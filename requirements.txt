# CoT Unfaithfulness Project - Complete Dependencies
# All phases (Phase 1, 2, 3, 4)
# Install: pip install -r requirements.txt

# Core ML (Phase 2, 3)
torch>=2.0.0
transformers>=4.39.0
accelerate>=0.27.0

# Mechanistic interpretability (Phase 3)
transformer-lens>=1.17.0
nnsight>=0.2.6
typeguard>=4.0.0  # Required by transformer-lens

# Data processing and analysis (All phases)
numpy>=1.20.0,<2.0.0  # Compatible with most packages
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Data formats (Phase 1, 2)
jsonlines>=4.0.0
pyyaml>=6.0

# Visualization (Phase 2, 4)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.0.0

# Utilities (All phases)
tqdm>=4.60.0

# Testing (All phases)
pytest>=7.0.0
pytest-cov>=4.0.0

# Note: For GPU support with PyTorch, the package will auto-detect CUDA.
# If you need a specific CUDA version, install torch separately first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Or for CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# For CPU-only (local development):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

